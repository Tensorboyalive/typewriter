# Supabase
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Admin passcode for Finances / Expenses gate.
# NOT a security boundary — RLS is the real boundary. This just keeps the PA
# out of the owner's money surfaces on a shared login.
VITE_ADMIN_PASSCODE=
